# Core LLM / embeddings client
openai>=1.40.0

# LangChain stack (document loading, embeddings wrapper, vector store wrapper)
langchain>=0.2.14
langchain-core>=0.2.38
langchain-openai>=0.1.22
langchain-chroma>=0.1.3

# Vector database used by langchain-chroma
chromadb>=0.5.5

# PDF parsing (imported as `fitz`)
PyMuPDF>=1.24.9

# Tokenizer used for chunking / token accounting
tiktoken>=0.7.0

# Numerical utilities used in metric computation
numpy>=1.26.4

# LLM-based evaluation metrics (Factual Correctness / RAGAS)
ragas>=0.2.6
