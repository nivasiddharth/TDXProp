# TDXProp
A retrieval-augmented pipeline that extracts missing Intel TDX security properties using multi-query expansion, benchmarked against LLM-only and standard RAG baselines across four TDX security domains.
